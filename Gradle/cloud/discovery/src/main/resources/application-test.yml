spring:
  config:
    activate:
      on-profile:
      - test

  devtools:
    livereload:
      enabled: false

  output:
    ansi:
      enabled: never


server:
  servlet:
    session:
      cookie:
        http-only: false
        secure: false
  
  http2:
    enabled: false
